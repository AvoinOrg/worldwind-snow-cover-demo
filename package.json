{
  "license": "ISC",
  "scripts": {
    "build": "rm -rf dist/ && parcel build --public-url . src/index.html && cp -a map/ worldwind/images/ dist/",
    "start": "parcel src/index.html"
  },
  "dependencies": {
    "@nasaworldwind/worldwind": "^0.9.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.4"
  }
}
