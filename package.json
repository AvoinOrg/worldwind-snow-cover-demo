{
  "scripts": {
    "build": "rm -rf dist/ && parcel build src/index.html && cp -a worldwind/images/ dist/",
    "start": "parcel src/index.html"
  },
  "dependencies": {
    "@nasaworldwind/worldwind": "^0.9.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.4"
  }
}
